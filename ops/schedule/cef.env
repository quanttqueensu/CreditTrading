# Human-owned knobs for the credit CEF discount sleeve.
#
# RUNG LADDER — raise ONE rung at a time, each a deliberate human edit:
#   RUNG-0  DRY_RUN=1  EXECUTION=simulator   compute + log targets only
#   RUNG-1  DRY_RUN=0  EXECUTION=simulator   live shadow ledger, no orders sent
#   RUNG-2  DRY_RUN=0  EXECUTION=ibkr        real orders to the IBKR PAPER account
#
# CURRENT RUNG: RUNG-2, re-armed 2026-07-31 after the halt described below.
#
# HALTED 17:03 ET, RESTORED 18:0x. The sleeve seeded `_live_positions` from the
# shadow ledger, which read flat while the account held $2.07M gross, so the next
# fire would have re-bought the entire $750k book against 164,500 CAD of excess
# liquidity at 83% margin use. Root cause was a missing config/costs.yaml entry
# crashing ops/ledger.py mid-write AFTER orders had transmitted.
#
# Safe to run again because `arm()` now gates every live session: it adopts
# quantities from ib.positions() while keeping the ledger's attribution, consults
# sibling book specs so a symbol another book also trades is never taken from the
# account net, and raises NotArmed rather than transmitting on unverified state.
# Verified 2026-07-31: this book arms clean, phase0 arms clean with all 14
# attributions correct.
#
# NOT IDEMPOTENT: each armed run stacks another order set at the broker, there is
# no dedupe. Cancel pending orders before re-running by hand. The scheduler fires
# once a day, so this only bites manual runs.
#
# ORIGINAL: CURRENT RUNG: RUNG-2. Paper account DUQ199038, no real capital.
# This sleeve cleared the full gate battery on 2026-07-31: PIT universe,
# 9/9 purged walk-forward blocks positive, bootstrap P(SR<=0)=0.000%,
# deflated Sharpe 0.956, Test 7 carry/beta 5/5 factor limits with R2 0.005.
#
# KILL RULE (pre-committed, reviewed at session 60 and not before):
#   (a) live net Sharpe < 0.0, or
#   (b) realised slippage > 2x modelled for 5 consecutive sessions, or
#   (c) top-minus-bottom discount spread < 12% (half its current 22.5%).

DRY_RUN=0
EXECUTION=ibkr
IBKR_CLIENT_ID=45

# THIS JOB MOVED TO THE MORNING (2026-09-11, W3 Part A). It now fires at 08:30
# and 12:00 and decides on the PREVIOUS trading day's pair, which is the same
# trade as the 20:00 decision it replaces -- same information, same MOC, same
# `band_frontier.evaluate` convention -- except the pair has been complete for
# ten hours instead of being raced for. The evening fire is now `cef_pm` at
# 17:30 (ops/schedule/cef_pm.env), which captures fills, writes tonight's pair
# and decides only when the morning could not. Which of the two is allowed to
# decide on any given day is ops/session_plan.py's answer, not a setting here.
#
# THE THREE LINES BELOW ARE NOW INERT FOR THIS JOB, AND MUST NOT BE DELETED.
# The morning session does not wait, so nothing reads NAV_DEADLINE on this
# path any more -- but two other things read this exact line and would break:
#
#   ops/promote.sh derives the session window it refuses to promote inside
#     ("16:30-$NAV_DEADLINE") by sed-ing this file. With the line absent it
#     reads unparseable and REFUSES EVERY PROMOTION
#     (ops/tests/test_promote_gate.py::..._unparseable).
#   ops/schedule/run_cef.sh, the manual evening wrapper, still sources it and
#     falls back to 21:30 -- an hour earlier than the two measured completions
#     of 22:43 and 21:45, i.e. it would stand down on a normal night.
#
# They also make the rollback one edit: put the 17:15 plist back and this job
# is the evening session again, unchanged. ops/doctor.py's stuck-session
# ceiling reads the INSTALLED plist rather than this line precisely so that it
# stays correct in both directions.
#
# SAME-DAY NAV (added 2026-09-08). The session fires at 17:15 but does not
# decide until every deployed name has TODAY's NAV; until then the last
# complete price/NAV pair was yesterday's, and every session before this date
# decided on it -- one day behind the backtest's convention. The wait polls
# yfinance (the panel's source) and CEFConnect; if the pair is not complete by
# NAV_DEADLINE the session stands down and alerts. It never trades on a lagged
# pair. IB Gateway auto-restarts at 03:00, so anything before ~02:30 is safe.
# Measured 2026-09-08: yfinance publishes the day's NAVs at ~22:45 ET, in one
# batch; CEFConnect later still. 23:30 leaves the 03:00 gateway restart clear.
# The evening decision is the fallback once the morning decision (P8.1) lands.
NAV_DEADLINE=23:30
NAV_POLL_SECONDS=900
NAV_WAIT_EXTRA=--no-cefconnect

# BOOK / BOOKS_ROOT deliberately absent (2026-09-10, prod/dev split). They used
# to pin absolute paths into ~/Desktop. Once the scheduler runs from ~/prod, an
# absolute dev path here would make the manual `ops/schedule/run_*.sh` wrapper
# write to a DIFFERENT book than the scheduled job does -- two ledgers claiming
# the same account. The wrappers already default them from their own location
# (REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"), and the launchd
# entry point derives them from its own REPO, so every path now follows the tree
# the code is running from. Set them only to point a one-off run somewhere
# unusual, and unset them again afterwards.
