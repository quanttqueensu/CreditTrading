# HALT — automated trading is blocked

`ops/preflight.py` reads this file before every scheduled session and will not arm live orders while it exists. Data collection and logging continue regardless: a halted book still records, it just does not trade.

Most recent halt first.

---

## 2026-09-09 09:37:36  broker arming failed — position attribution is ambiguous

- **source**: `run_book.arm`
- **state**: trading is BLOCKED until this file is cleared

`arm()` could not explain the account with the registered sleeves:

  - JNK: account holds +26 and null_trader trades it, but so does another book and this sleeve's ledger has no entry — cannot tell how much of it is ours

No order was transmitted. Resolve the attribution (usually by rebuilding the shadow ledger from broker fills) before the next session.

To clear once the cause is genuinely fixed:

    python3 -c "from ops.halt import clear_halt; clear_halt('what you fixed')"

---



CLEARED 2026-09-09 13:55:51: Cleared at the team lead's request 2026-09-09. Cause fixed: null_trader JNK claim set to an explicit 0.0 in phase0 _attribution.json (it sold all 961 on 09-08; the account's 26 JNK are bench_b6's per its ledger). Also repaired the six re-seeded ledger manifests and fixed reset_epoch.py.
