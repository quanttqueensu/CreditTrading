# P6.3 — Group-specific fair value: a conditional level for each kind of fund

**Lever:** IC via M4. The only untested route to a better level is a
*conditional* one. **Trials:** 1 per group tested (up to 4; count them).
**Touches the live book:** after the 27-name check and pre-registration.

---

**Amended 2026-09-08 (P8.6):** the decision rule also requires the 2023–2026 time-holdout row to agree in sign at matched turnover, with every estimated quantity fitted before 2023-01-01; the 27 remain the second check. This prompt increments the **CEF** counter.

## Paste from here

You are a quant researcher on the QUANTT CEF book. Read, in order:

1. `results/cef/ESTIMATOR_NOTE.md`: the unobserved-components (Kalman)
   level lost to a plain 63-day window, and the 63-day window then failed
   out of sample on turnover. **A better unconditional level does not
   help.** Do not rebuild it.
2. `results/cef/DIST_CUT_NOTE.md` "What this changes": "the right
   response to a cut is not to remove the fund — it is to move its fair
   discount." The rolling mean takes a year to absorb a re-rating.
3. `docs/PER_NAME_ARCHITECTURE.md` §5: muni (tax-sensitive retail,
   tender-option-bond leverage, state-specific); multi (PIMCO-dominated,
   distribution-driven flows, persistent premiums); hy (credit-beta
   sensitive); loan (floating rate; n = 1 in the 17).
4. Pontiff (1996, QJE): discounts widen with arbitrage costs and with
   interest rates; Cherkes, Sagi & Stanton (2009, RFS): the discount
   prices liquidity benefits against fees. Both say the *fair* discount
   is a function of observable state, not a constant.
5. P6.1's note: the within-group IC by group is the baseline each group's
   conditional signal must beat.
6. `data/rv/etf_ohlc.parquet` (AGG, BKLN, EMB, GOVT, HYG, IEF, JNK, LQD,
   SRLN, TLT, VCIT…; **no muni ETF**), `data/financing_curve.parquet`
   (SOFR/EFFR base rate), `scripts/fetch/refresh_market_feeds.py`,
   `scripts/cef/band_frontier.py`.

### The idea, and the trap

The live fair value is $\mu_i = $ the 252-day rolling mean of the
fund's own discount. A muni CEF's *true* fair discount moves when the
muni/Treasury ratio moves (muni bonds cheapen, muni CEFs cheapen more),
when short rates move (its leverage is financed at SIFMA; net income
falls when SIFMA rises, distributions get cut, the discount widens), and
in tax season. The rolling mean sees all of that as dislocation and
trades against it for months. A conditional level
$\theta_{g,t} = a_g + b_g' x_{g,t-1}$ absorbs it, and the residual is the
part that is actually noise-trader dislocation.

The trap: this is the Kalman argument with a different estimator, and the
Kalman lost. It lost because a fast-adapting *unconditional* level is
just a shorter window, which trades more. A *conditional* level is
different only if the conditioners carry information the rolling mean
does not. That is the thing to test, and the negative control (feed a
group the wrong group's conditioners) is what tells the two apart.

### The conditioners, pre-declared per group (write them down before running)

**muni** (6 in the 17; 10 in the 27):
- muni/Treasury relative value: 21-day return of a national muni ETF
  minus IEF. Fetch MUB (and PZA as a check) into the ETF panel via
  `refresh_market_feeds.py` with a source note; do not hand-copy.
- leverage cost: 63-day change in the SOFR base rate from
  `financing_curve.parquet` (SIFMA weekly from sifma.org if you can
  fetch it reproducibly; SOFR is the fallback, stated).
- tax-season indicator: November–January dummy.
- group NAV momentum: trailing 63-day mean NAV return of the muni group.

**hy** (2 in the 17; 4 in the 27):
- credit spread proxy: 21-day return of HYG minus LQD.
- credit beta sentiment: 21-day return of HYG.
- equity vol: 21-day change in VIX (fetch ^VIX via yfinance into the
  feed; source note).

**multi** (8 in the 17: PDI PTY PDO PCN PHK PFN are PIMCO; DSL DoubleLine;
BIT BlackRock; state that the group is mostly one family; 3 in the 27):
- family co-movement: mean z of the *other* PIMCO funds (excluding the
  name itself; a PIMCO-premium regime factor).
- premium persistence: the group's trailing 252-day mean premium.
- distribution-coverage proxy: if PIMCO's monthly UNII/coverage report
  can be fetched reproducibly, the fund's UNII per share over its
  distribution; otherwise omit and say so. Never hand-enter.

**loan** (1 in the 17; 6 in the 27): estimate on the six loan names in
the 27 only:
- SOFR level and 63-day change.
- 21-day return of BKLN or SRLN.
JFR inherits the pooled treatment; that is explicit in code.

Each conditioner has a stated sign expectation (e.g. muni/Treasury
cheapening → muni discounts wider → negative coefficient on $d$). A
fitted coefficient with the wrong sign is a flag, reported, not
silently used.

### The construction, no lookahead

Per group, on a trailing 504-session window ending $t-1$, refit monthly:

1. Pool the group's names: regress each name's discount $d_{i,s}$ on the
   group conditioners $x_{g,s-1}$ (lagged one day) with a name fixed
   effect: $d_{i,s} = a_i + b_g' x_{g,s-1} + e_{i,s}$.
2. Conditional fair value at $t$: $\theta_{i,t} = \hat a_i + \hat b_g'
   x_{g,t-1}$.
3. Conditional dislocation $u_{i,t} = d_{i,t} - \theta_{i,t}$; z on
   $u$ with its **own** trailing 252-day sd (shift 1), clip ±4. The mean
   is already removed by $\theta$; do not demean again with a rolling
   mean or you have rebuilt the live signal plus noise.
4. Everything downstream unchanged: cross-sectional demeaning across the
   whole book, normalisation, vol scalar, band.

Alignment assertions on every window and lag. Report the fitted $b_g$
with signs and t-stats per era, and the $R^2$ of the conditioners in
explaining the discount level per group (if it is under 5%, the
conditioner set is weak and the result will be noise; say so before the
IC test).

### The tests

1. **The 27 first** (P1.6 step 2 must be written down; the 27 include an
   emd group, EDD EMD EDF TEI MSD, which gets no conditioners here and is
   treated as the negative control that receives the pooled level).
   Per group in the 27: IC of the conditional z vs the live z, 2d/T+1,
   non-overlapping, by era, with the difference and its SE. Recorded
   before the 17 are touched.
2. **The 17**, same IC table by group, then the harness turnover-matched
   (band 4.8%) for the whole book with the conditional level applied to
   the groups that passed on the 27 and the live level elsewhere.
3. **The negative control:** feed each group the *other* groups'
   conditioners (muni gets HY's; HY gets muni's), same estimation. If the
   IC improvement is similar, the gain is from a smoother level, not from
   information, and the group signal claim fails.
4. **Post-cut behaviour:** the mean conditional z of cut funds at t+21,
   t+63, t+126 after a distribution cut (from `cef_dist_features.parquet`),
   against the live z's −0.286 / −0.341. A conditional level that still
   reads cut funds as cheap for six months has not solved the thing the
   Kalman was built for.

### Decision rule

Per group: adopt the conditional level for that group if, on the 27, its
IC beats the live IC by more than 2 SE and the negative control does
not; and on the 17 the same sign holds; and the whole-book harness
shows net@15 and net@30 not worse at matched turnover. Each group tested
is one trial regardless of outcome. Groups that fail keep the rolling
mean.

Pre-register per group (`PREREG_GROUP_LEVEL_<group>_<date>.md`): the
conditioners, their signs, the window, the falsifier. Spec key
`level_model: {"muni": "conditional_v1", ...}`, absent = rolling mean,
no-op proven.

### Deliverables

- `scripts/cef/group_fair_value.py`, the fetch additions with source
  notes, `results/cef/GROUP_FAIR_VALUE_<date>.md` with the 27 results
  first and the 17 second, per-group pre-registrations for adopted
  groups, trial counter updated by the number of groups tested.

### Do not

- Do not add a conditioner after seeing a result.
- Do not use per-name coefficients on the conditioners (the group
  pools; the name gets a fixed effect only).
- Do not use the smoother (future data) anywhere; the level at $t$ uses
  data to $t-1$.
