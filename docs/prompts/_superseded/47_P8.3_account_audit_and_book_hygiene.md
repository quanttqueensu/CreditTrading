# P8.3 — Account capability audit, and retire the null trader

**Lever:** prerequisites. Nothing in Sections 7–8 can start until we know what
the paper account can do, and the account carries a control book whose job
is finished. **Trials:** 0. **Touches the live book:** no; it winds down a
different book. Standing decisions (Part I): options permissions unknown;
keep the benchmarks, retire the null trader.

---

**Amended 2026-09-09:** `ops/HALT.md` is global — on 2026-09-09 the *benchmarks*
book halted over ANGL (its own pre-epoch fill landing after its re-seed) and
that halt would have blocked the CEF session's preflight at 22:45. Add to
this prompt: **scope halts per book** (`ops/HALT.md` keeps blocking everything
only when written by the book itself or by a human; a sibling book's arm
failure writes `ops/halts/HALT_<book>.md` and blocks only that book), and
make `preflight` read both. Also: `capture_fills` attributes by symbol, so
the null trader's 09-08 fills in JNK/LQD/USHY/SHYG/VCIT/EMB were also written
into bench_b6's `broker_fills.csv`; retiring the null trader removes the
cause, but dedupe the benchmark fill files by execId against the null
trader's before the record is used for anything.

## Paste from here

You are working in the QUANTT credit CEF repo. Two housekeeping questions
gate the options programme and simplify every reconciliation. Read:

1. `ops/books/phase0_book.json`, `ops/schedule/phase0.env`,
   `src/deploy/sleeves/null_trader.py` — the control book: 6 ETF positions,
   ~$310k gross, built 2026-07 to measure execution cost against a book with
   no signal. P1.3 now measures cost from our own MOC fills, so the control
   has done its job.
2. `ops/books/benchmarks_book.json` — the five reference books. They stay.
3. `ops/reconcile_orders.py::ALL_BOOKS`, `ops/books/*/_attribution.json` —
   shared tickers (LQD, VCIT, USHY, EMB, HYG, SHYG are held by the null
   trader **and** bench_b6) are attributed by ledger, so winding one book
   down must remove only its share.
4. `src/deploy/portfolio.py` — "A killed sleeve keeps advancing FLAT until
   its position is wound down": the disable path already exists.
5. `docs/prompts/00_README.md` Parts E and I.

### Part 1 — What can the paper account do?

Write `ops/account_audit.py`, read-only except for `whatIfOrder`, run
against the gateway, output a table and a verdict per capability:

| capability | how to test | needed by |
|---|---|---|
| **Options trading permission** | `whatIfOrder` on a 1-lot HYG put (nearest monthly, 25-delta). A permission error (IB error 10xxx / "not permitted") is the answer; a margin response means yes. Never transmit. | P7.2, P7.3, P8.4 |
| Option **market data** | `reqMktData` on that contract with `reqMarketDataType(3)` (delayed) and (1) (live): which returns a bid/ask; note the subscription message. | P7.3 marks; P8.4 |
| **Historical option bars** | `reqHistoricalData` on the same contract: how far back IB serves, at what bar size. | P8.4 fallback data |
| **Historical equity bars** for the 44 | daily, full history: confirm depth (P8.2 depends on it). | P8.2 |
| **Margin type** | `accountSummary` tags incl. `AccountType`; Reg-T vs portfolio margin changes what a 20% vol target costs (P1.5). | P1.5 |
| **Short sale availability API** | is `reqHistoricalData` `whatToShow="REBATE_RATE"`/`"FEE_RATE"` served on the paper account? It would replace the borrow CSV. | P1.1, P3.5 |
| Client id headroom | list every client id in `ops/schedule/*.env`, `config/.env`, dashboard, tools; confirm no collisions with P8.1's new jobs. | P8.1 |

Write the result to `results/ops/ACCOUNT_AUDIT_<date>.md` with the exact IB
messages. If options permission is absent, the note says so in the first
line and lists the human step (IBKR Client Portal → Settings → Trading
Permissions → Options, US) — the prompts in Section 7–8 refuse to start
until this note says "permitted".

### Part 2 — Retire the null trader, cleanly

The order matters because of the shared tickers.

1. **Freeze attribution first.** Snapshot `_attribution.json` and every
   ledger's positions for the six shared ETFs; compute the null trader's
   share of each from *its* ledger. Write the table into the note before
   anything moves.
2. **Disable the sleeve** in `phase0_book.json` (`"enabled": false`) and
   run the phase0 job once, armed: the portfolio path emits FLAT targets
   for the null trader's held names and the broker path sells *its share
   only* (the tag book is per sleeve — verify `place_targets` diffs against
   `_live_positions["null_trader"]`, not the account net). Confirm from
   the fills that the benchmark books' positions in LQD/VCIT/USHY/EMB/HYG/
   SHYG are unchanged to the share.
3. **Reconcile** with `reconcile_orders --check-broker` across all books:
   zero divergence on the six shared names.
4. **Unschedule:** `launchctl bootout` of `com.quantt.phase0.daily`, remove
   the plist, keep `ops/books/phase0_live/` as an archive (read-only;
   `chmod -w`), remove `phase0` from the watchdog's expected jobs and from
   `ALL_BOOKS`' *live* list while keeping it in the attribution history
   readers, and record the retirement in `docs/RESEARCH_STATE.md` with the
   control's final measurement (its realised cost, from P1.3's method).
5. Dashboard: the null trader disappears from Positions attribution; the
   benchmark sleeves remain.

### Deliverables

- `ops/account_audit.py`, `results/ops/ACCOUNT_AUDIT_<date>.md`.
- The null trader's wind-down note (`results/ops/NULL_TRADER_RETIRED_<date>.md`)
  with the before/after table on the shared tickers and the final cost
  measurement.

### Do not

- Do not transmit any option order; `whatIfOrder` only.
- Do not flatten a shared ticker at the account level. Only the null
  trader's own tag book is driven to zero.
- Do not delete the phase0 ledger or its fills; they are the control's
  record.
