# P8.1 — Decide in the morning, capture in the evening: the timing architecture

**Lever:** TC and reliability. The session stops depending on when five fund
sponsors publish. **Trials:** 0. **Touches the live book:** yes — it changes
*when* the deployed sleeve is run, not *what* it computes. **Scheduled for
Thursday 2026-09-10** (the prod/dev split lands Wednesday; the first morning
session runs on prod, not on a tree that just moved). **Standing decision
(Part I):** morning primary, evening fallback.

---

## Paste from here

You are working in the QUANTT credit CEF repo on the PROD checkout. Read, in
order:

1. `docs/prompts/00_README.md` Parts D, E, I.
2. `~/Library/Application Support/quantt/launch_job.py` — the session
   contract (REFRESH → PREFLIGHT → TRADE → CAPTURE), the same-day guard, the
   `wait` step added 2026-09-08, and `halt_mod.beat`.
3. `ops/schedule/logs/cef_2026-09-08.log` — the first night of the NAV wait.
   Read the poll lines: at 19:27 ET neither yfinance nor CEFConnect had a
   single one of the 17 NAVs for the day. Sponsors publish between roughly
   18:00 and 22:00 and the redistributors (Nasdaq Fund Network → every
   vendor) land overnight. An evening decision on today's pair is a race we
   do not control.
4. `scripts/cef/band_frontier.py::evaluate` — `H.shift(2)`: decide on the
   pair at *t*, MOC fill at the *t+1* close, earn from *t+1* to *t+2*.
5. `src/deploy/exec_ledger.py` — how the shadow ledger dates a decision and
   its fill (`decision_date`, `fill_date`), because the ledger's fill date
   must equal the broker's fill date or the P&L record is wrong by a day.
6. `src/deploy/broker/ibkr.py::arm` and `place_targets`; `ops/capture_fills.py`.
7. `~/Library/LaunchAgents/com.quantt.*.plist`, `ops/schedule/install.sh`,
   `com.quantt.awake.plist` (caffeinate window 09:00–22:30 today).

### The insight, in one line

A decision made at 08:30 on day *D* from the completed pair of *D−1*, placed
as MOC for *D*'s close, is **the same trade** as a decision made at 20:00 on
*D−1* from the same pair — identical fill, identical information — except
that at 08:30 the pair is complete on every vendor and the gateway has been
up since its 03:00 restart. The publication race disappears. It is also
exactly the backtest's convention: pair at *t*, fill at *t+1*.

The only thing the morning cannot do is capture the previous day's fills,
because IB's `reqExecutions` serves the current TWS session and the 03:00
restart empties it. So capture moves to a small evening job. Two jobs, two
questions: the morning decides, the evening records.

### Task

**1. Morning session `cef` at 08:30 ET.**

- `launch_job.py`: the `cef` job's `asof` becomes the **previous trading
  day** (`nyse_calendar --prev today`), not today. The refresh requires the
  pair complete for that date (`fetch_daily --require-asof <prev>`), the
  sleeve decides on it, and the shadow ledger's `fill_date` for those orders
  is **today** — verify by reading how `exec_ledger` derives the fill date
  from `asof` (decision *t* → fill *t+1*; with `asof = prev`, *t+1* is
  today). If the ledger derives the fill date from the calendar, confirm it
  uses the NYSE calendar and not `+1 day`.
- `arm()` adopts broker truth as it does now. **Before placing anything, it
  cancels any resting order carrying this book's client id** (from the
  evening fallback or a failed run) and logs each cancellation. This is the
  fix for the 2026-09-04→08 incident and it lives at arm time, not in a
  separate tool.
- The **same-day guard becomes a same-pair guard**: the heartbeat records
  `pair_date`; an armed run for a `pair_date` that already has an armed
  heartbeat is refused. This is what makes morning-plus-evening safe: the
  evening fallback on *D* decides on pair *D*; the morning of *D+1* would
  decide on pair *D* again and must be refused by the guard, not by luck.
- A **retry** fire at 12:00 ET runs the same job. The same-pair guard makes
  it a no-op if 08:30 armed; if 08:30 failed (gateway down, NAV incomplete),
  12:00 is a second chance well inside the 15:50 MOC cutoff. Both fires log
  to the same day file.
- `NAV_DEADLINE` semantics: in the morning the wait is short — poll for at
  most 45 minutes (to 09:15) for a straggler NAV; then stand down for that
  fire and let 12:00 try again.

**2. Evening capture `cef_pm` at 17:30 ET.**

- A `JOBS["cef_pm"]` entry with `capture_only: True`: no refresh, no trade;
  phase 4 (`capture_fills --slippage`) and the heartbeat key `cef_pm`. It
  runs after the 16:00 fills and before the 03:00 restart. It must run even
  on a day the morning did not arm (there may be fills from a resting
  order).
- The **evening fallback**: if today's heartbeat shows no armed `cef` for
  pair *D−1* (the morning and the retry both failed), `cef_pm` runs the
  existing wait-for-today's-NAV path and, if the pair completes by 21:30,
  decides on pair *D* for the *D+1* close — the 2026-09-08 behaviour, now
  the exception. It writes `pair_date = D` so the next morning's guard
  refuses the duplicate.

**3. Scheduling.**

- Plists: `com.quantt.cef.daily` → 08:30 and 12:00 Mon–Fri; new
  `com.quantt.cef.pm` → 17:30 Mon–Fri. Render with `install.sh`; the human
  enables (`--enable`). `com.quantt.awake`: caffeinate from **08:00**
  (`StartCalendarInterval` 08:00, duration to 22:30). Doctor must check the
  new plists and the new window.
- Watchdog: expect a `cef` heartbeat dated today by 09:30 (alert at 09:45 if
  none), and a `cef_pm` heartbeat by 18:30.
- Phase0 (09:35) and benchmarks (17:25) keep their slots; confirm no client
  id is shared with the new jobs (`ops/schedule/*.env`).

**4. Dashboard.** `/api/trades`: `decision_date` = today, `fill_date` = today
(morning path) — the Trades header reads "MOC decided this morning on the
<prev> pair, filling at today's close". After 16:00 the panel should say
"filled; see the blotter" rather than keep showing the list. Session watch
(P4.3) follows the 08:30 session, not 17:15.

**5. Prove it before Thursday, on dev.** With `DRY_RUN=1`: run the morning
path against a Wednesday-morning snapshot of the panel and confirm (a) the
required pair date is Tuesday, (b) the dry-run targets equal what the
evening path would have produced on Tuesday's pair, byte for byte, (c) the
ledger dry-run dates the fill Wednesday. Run the same-pair guard test: two
runs, second refused. Run the evening fallback with a fake "no armed
heartbeat" and confirm it takes the wait path.

### Acceptance

- Thursday 08:30: the log shows pair 2026-09-09 complete, arm, cancel-stale
  (zero or more), orders placed, heartbeat `cef` armed with `pair_date =
  2026-09-09`. 12:00: refused by the same-pair guard. 17:30: fills captured,
  slippage row written, heartbeat `cef_pm`.
- No session decides on a pair older than one trading day, ever again,
  except through the logged evening fallback.

### Do not

- Do not keep `asof = today` in the morning and let the sleeve "fall back to
  the last complete pair". The ledger would date the fill tomorrow while the
  broker fills today. The asof *is* the previous trading day.
- Do not remove the evening wait code; it is the fallback.
- Do not fire the morning job before 08:15 — IBC's 03:00 restart plus login
  must be verifiably complete (doctor checks the gateway's API answers).
