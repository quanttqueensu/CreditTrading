# P1.6 — Breadth: screen on ticks, and run the 27 untouched names before promoting any

**Lever:** BR. **Trials:** 1 (the promotion). **Touches the live book:**
only after the 27-name comparison is written down.

---

## Paste from here

You are a quant researcher on the QUANTT CEF book. Read, in order:

1. `docs/PLAN.md` §4.3 (buy breadth by screening on the right thing:
   `min_adv_usd = $3m` binds, median eligible universe is 8; at $1m ADV, 37
   names at 0.44% participation; "screen on ticks, not ADV"; five names
   clear $1m ADV and are cheaper per tick than the live median: DBL, NXP,
   BGH, ISD, GHY; four of five are non-muni), §7.2 (the 27 untouched names
   are independent of our selection process), §7.3 (the sequencing
   conflict: the candidates are among the 27; **open the 27 once on the
   combined specification, then promote**; "no name enters the live
   universe before the comparison on the 27 is run and written down").
2. `data/cef/cef_universe.csv` — 44 names with group, ADV, last price.
3. `scripts/cef/band_frontier.py` — `UNIVERSE`, `MIN_ADV`, `MIN_NAMES`,
   and the harness.
4. `dashboard/server.py::_decompose` — BR_eff and the PC2 classifier, so
   you report breadth the same way the dashboard does.
5. `config/costs.yaml` — per-name half-spread; the convention is
   half-spread in bp = 0.5 cent / price, so "tick cost" here means the
   half-spread of a one-cent market.

### Why breadth is the lever, and why ADV is the wrong screen

$IR \approx IC\sqrt{BR}$. IC is settled; the only input that raises the
ratio without the signal getting better is BR. But the book's effective
breadth is 1.2–2.2, not 17, because the eligible set is small and
muni-heavy. Adding names that are **not** munis both raises the name count
and dilutes the dominant factor.

We do not move markets (0.7% of ADV at the live screen); we pay ticks. A
one-cent spread on a $4.69 share is 10.7bp against a 32.6bp breakeven, a
third of the edge in the minimum increment. On a $17 share it is 2.9bp. The
right screen is the cost per unit of exposure, which is tick cost, plus a
participation guard that ADV supplies at a much lower level than $3m.

### Task, in this order

**1. The tick screen.** `scripts/cef/tick_screen.py`: for all 44 names,
per date since 2005:

- `half_spread_bp = 1e4 * 0.005 / close` (state the convention),
- `adv_usd` = 63-day mean of close × volume, shifted one day,
- `participation` at the strategy's typical trade size: annual turnover
  17.6 × NAV / 250 sessions / n_eligible, as a fraction of ADV,
- `nav_age` from the NAV panel.

Eligibility, **derived not swept**: `half_spread_bp ≤ 32.6 / 3` (the
minimum increment may cost at most a third of breakeven; write down why a
third and not a quarter or a half: it is the point at which one tick of
adverse rounding on entry and exit together consumes two-thirds of the
edge), `participation ≤ 1%` (five times below the cost model's own 5%
guard), `adv_usd ≥ $1m` as the floor below which the close is too thin to
print. Report eligible count per date under the old and new screens, and
which names change status.

**2. Run the 27 first, once.** Build the 27-name universe (the 44 minus
the frozen 17). Run the **current live specification unchanged** (z 252,
band 4.8%, vol target 6%, MOC shift 2, `min_names` 6, the *new* tick
screen for eligibility since ADV $3m excludes almost all of them) as its
own book through the harness. Record in `results/cef/HOLDOUT27_<date>.md`:
gross, ann return, vol, turn/yr, hold, net@5/15/30, by era, BR_eff, PC2
share, and the per-name leave-one-out. Also the IC at 2d/T+1.

Rules for this step: **one run**. No variants. Whatever it prints is
recorded. Do not read it before the script has written the file. This
number is the independent check that the live specification is not an
artefact of the 17, and it is consumed the moment any of the 27 is traded.

**3. Only then: candidate sets.** Evaluate the live 17 plus candidate
additions as *group-balanced sets*, not a name list: for set sizes 3, 5,
8, choose the eligible non-muni names with the lowest tick cost first,
and report for each set:

| set | n names | median eligible | gross SR | turn/yr | net@15 | net@30 | BR_eff | PC2 share | mean tick bp | borrow-weighted fee |

against the live 17. Turnover-matched. The claim to test is that BR_eff
rises and PC2 falls at a tick cost no worse than the live median. Also
run the same sets with muni additions (NXP is a muni) to show the
contrast.

**4. Pre-register the promotion** if a set clears: `results/cef/
PREREG_BREADTH_<date>.md`, listing the names, the screen that admitted
them, the expected BR_eff and PC2, and the falsifier (BR_eff below the
live 17's after 20 sessions). Update `UNIVERSE` in the frozen spec and in
`band_frontier.py` together. Trials +1.

### Decision rule

Promote the smallest set that raises BR_eff by at least 1.0 and lowers
PC2 share by at least 15 points at matched turnover, net@15 no worse than
live, net@30 no worse than live. If no set does, record it and stop.

### Deliverables

- `tick_screen.py`, `HOLDOUT27_<date>.md` (written before step 3 runs),
  `results/cef/BREADTH_<date>.md` with the set table, the pre-registration
  if applicable.

### Do not

- Do not run step 3 before step 2's file exists.
- Do not sweep the tick threshold or the ADV floor.
- Do not add a name whose NAV publishes late (nav_age > 1bd routinely);
  it is a blind signal.
