# P1.8 — The price / tick-cost tilt: measured +0.16 net, recommended "Adopt", never deployed

**Lever:** M8 (fund health) and TC (cost per unit exposure). **Trials:** 1.
**Touches the live book:** after the 27-name check and pre-registration.

---

## Paste from here

You are a quant researcher on the QUANTT CEF book. Read, in order:

1. `results/cef/research/NICHE_EDGE_RESEARCH.md` §1c and §4 item 1. On
   2026-07-31, in the 5-day-hold era: weighting positions by price
   (equivalently by 1/half-spread) moved net Sharpe 0.82 → 0.98, gross
   1.26 → 1.36, modelled cost 265 → 215bp/yr, alpha t 3.62 → 4.36, 5/5
   factor limits, and was marked **Adopt** "with the caveat that part of
   the gain is a fund-quality proxy rather than pure execution". It was
   never deployed; the live sleeve is equal-weighted in z.
2. `docs/PLAN.md` §4.1 (PHK fails four independent measures: slowest
   reversion, widest discount sd, most expensive tick at 10.65bp, leave-
   one-out Sharpe rises without it) and `docs/PER_NAME_ARCHITECTURE.md` §4
   ("an expensive name is not a bad name, it is a slow one"; PHK's derived
   band is 7.35%).
3. `scripts/cef/band_frontier.py` and `config/costs.yaml`.
4. `docs/PLAN.md` §7.3 and P1.6: the 27 are the comparison set for any
   specification change.

### The mechanism, in two parts that must be separated

**Cost.** A one-cent spread is 22bp on PHK at $4.50 and 6.5bp on PDI at
$15.33. Equal z-weights spend a third of the edge on the minimum increment
in the cheapest names. Tilting exposure toward high-priced names cuts cost
per unit exposure with no change to the signal. This is TC.

**Fund health.** Gross Sharpe also rose (1.26 → 1.36), which cost cannot
explain. Low-priced CEFs are typically the ones that have eroded NAV
through return-of-capital distributions; the price level proxies for
capital erosion, and eroded funds revert worse. This is a weak M8 alpha,
and it is also a **confound**: a price tilt might be a long-quality bet in
disguise. The July note checked the carry/beta factors (5/5 pass, R²
0.003); it did not check a *CEF-internal* price factor.

### The construction, one derived form

$$w_i \propto -(z_i - \bar z) \cdot g_i,\qquad
g_i = \frac{1}{hs_i} \Big/ \operatorname{median}_j \frac{1}{hs_j},\qquad
hs_i = \frac{0.005}{P_i}$$

Then demean again (the tilt breaks dollar-neutrality), normalise to unit
gross, vol-scale, min-weight, band, exactly as today. $g$ is the inverse
half-spread normalised to a median of 1, so the tilt is scale-free and
the units of $z$ are unchanged. **Do not also test $g = P_i$,
$\sqrt{P_i}$, or capped variants.** The July note already showed price and
$1/hs$ are arithmetically the same thing; one form, derived from the cost
mechanism.

Interaction with the band: the tilt changes target weights, so the
band's turnover changes; turnover-match.

Interaction with borrow: high-priced names in this universe are the PIMCO
premiums (PTY, PDI) which are cheap to borrow, and the low-priced are PHK
(cheap) and HYT (expensive). Report the borrow drag before and after.

### Method

**1. Run on the 27 first** (P1.6 step 2 must already be written down, or
do it here under its rules): the live spec with and without the tilt, one
run each, turnover-matched, recorded before the 17 are touched.

**2. Then the 17**, full harness: gross, turn, hold, net@5/15/30, net of
borrow, by era, BR_eff, PC2 share, leave-one-out with and without PHK.

**3. Separate cost from health.** Three additional rows, all on the 17:
- (a) the tilt applied only to the **execution** (weights unchanged, but
  turnover charged at the tilted cost): isolates the cost part;
- (b) regress the tilted-minus-untilted daily return on a "high price
  minus low price" CEF factor (equal-weight top-third by price minus
  bottom-third, dollar-neutral, rebalanced monthly): the alpha of the
  difference after that factor is the part that is not a price bet;
- (c) the negative control: tilt by a **random permutation** of $g$
  across names (10 draws). If permuted tilts help, the gain is
  concentration, not cost.

**4. The PHK question** answers itself: report PHK's weight under the tilt
(it should fall by roughly the ratio of its tick to the median, ~4×) and
the leave-one-out Sharpe with it kept at that weight. If the tilt
achieves what exclusion would without excluding, the four-measure case
against PHK is closed by construction rather than by a universe edit.

### Decision rule

Adopt if: the 27 show the same sign of improvement; net@15 and net@30 on
the 17 improve at matched turnover; the factor-adjusted alpha in (b) is at
least half the raw improvement (the tilt is not merely a price bet); and
the permuted control is materially worse than the true tilt. Otherwise
record and stop.

### Pre-registration and trial accounting

`results/cef/PREREG_TICK_TILT_<date>.md`: one spec key
`weight_tilt: "inverse_half_spread"` (absent = off, no-op proven),
expected turnover change, expected PHK weight, falsifier (realised cost
per unit turnover, from P1.3, not lower than before within 40 sessions).
Trial counter +1.

### Deliverables

- `scripts/cef/tick_tilt.py`, the sleeve change behind the key, the two
  results notes (27 first, then 17), the pre-registration if adopted.
