# P0.5 — Reconcile the risk-control keys and replace the P&L kill trigger

**Lever:** governance. Real capital arrives this year; the "no real capital
at risk" justification for inert controls is on a clock. **Trials:** 0.
**Touches the live book:** spec keys and code paths; controls stay unarmed
until a human flips the switch.

---

## Paste from here

You are working in the QUANTT CEF repo. Read, in order:

1. `ops/specs/cef_discount.frozen.json` — the `risk` block
   (`kill_drawdown: 0.18`, `halve_drawdown: 0.12`,
   `max_gross_exposure_usd`) and the `kill_rule` string: "Review at 60 live
   sessions, not before. KILL if any of: (a) live net Sharpe < 0.0; (b)
   realised slippage > 2x modelled for 5 consecutive sessions; (c)
   cross-sectional top-minus-bottom discount spread falls below 12%".
2. `src/deploy/risk.py` — `evaluate_sleeve()` reads
   `sleeve.risk.get("max_drawdown_kill_pct")`. That key does not exist in
   the spec. Neither `kill_drawdown` nor `halve_drawdown` is read by any
   code path (grep the repo to confirm).
3. `src/deploy/sleeves/cef_discount.py::risk_check` — returns `OK`
   unconditionally with a "WATCH" message at −12% and −18%. The docstring
   records the standing instruction of 2026-07-31.
4. `ops/books/cef_discount_book.json` — `book_drawdown_suspend_pct: 99.0`
   and the UNITS FIX note (it was 0.99, which `risk.py:152` read as 0.99%,
   the tightest limit in the book, breached at −1.84% on 2026-08-31).
5. `docs/PLAN.md` §7.4 — the two housekeeping items. The second says the
   existing kill clause (a) "fires 34% of the time on a strategy whose true
   Sharpe is exactly the 0.82 we believe", and proposes replacing it with
   the cost trigger of §7.1.
6. `docs/SYSTEM_AND_STRATEGY.md` §4 item 5, §5.
7. `dashboard/server.py::api_risk` and the Controls tab.

### Why a P&L kill rule is the wrong instrument here

Lo (2002): the standard error of an annualised Sharpe over n daily
observations is roughly √((1 + SR²/2)/n) · √252. Over 60 sessions that is
about 2.05 for SR near 0.5. A rule "kill if SR < 0 at session 60" on a
strategy with true SR 0.45 fires with probability Φ(−0.45/2.05) ≈ 41%; the
plan's 34% used 0.82. Either way the rule kills a working strategy more
often than not by chance, and would never have caught the thing that
actually threatens this book, which is cost.

Cost converges ~60× faster. At ~15 fills a session and ~6.5bp
session-level dispersion, 60 MOC sessions give SE ≈ 0.84bp against a 32.6bp
breakeven. That is a trigger that can actually fire for a true reason.

### Task

**1. One set of key names.** Decide on the names the spec already declares:
`kill_drawdown` and `halve_drawdown` (fractions, 0.18 and 0.12). Make
`src/deploy/risk.py` read those and delete the dead `max_drawdown_kill_pct`
lookup. Document the keys and their units in `src/deploy/sleeve.py`'s
module docstring next to `RiskVerdict`. Grep every spec under `ops/specs/`
and every sleeve for the old key and update them so no spec silently loses
a control.

**2. An explicit arming switch.** Add to the spec's `risk` block:

```
"controls_armed": false,
"_controls_armed_note": "false = observe-only (2026-07-31 standing
instruction: the paper deployment exists to generate data). true = HALVE
at halve_drawdown, KILL at kill_drawdown, computed on broker-confirmed
NAV only. Flipping this is a governance act recorded in RESEARCH_STATE."
```

With the key absent or false, behaviour is byte-identical to today (prove
it: run the sleeve's risk_check and `risk.evaluate_sleeve` on the current
ledger before and after). With it true, `risk_check` returns HALVE / KILL
at the thresholds.

**3. Drawdown on real sessions only.** The drawdown fed to the controls
must be computed on the broker-confirmed NAV path, not the modelled one.
Use the classification `dashboard/server.py::_provenance` builds from
`broker_fills.csv` (move that function into `ops/` so both the dashboard
and `risk.py` import one implementation; the dashboard must not own logic
the executor depends on). A drawdown that includes modelled sessions is
labelled "modelled" and never triggers.

**4. Replace kill clause (a) with the cost trigger.** New `kill_rule`
text, and a `ops/kill_rule.py` that evaluates it from files, no broker:

- (a′) **Cost:** mean implementation shortfall over the last 40 MOC
  sessions (from P1.3's `results/cef/shortfall_log.csv`; until it exists,
  from `slippage.csv` restricted to MOC sessions, i.e. excluding
  2026-07-31), with Lo-style SE, exceeds the 32.6bp breakeven at 95%
  confidence, AND at least 20 sessions are in the window.
- (b) unchanged: realised slippage > 2× modelled for 5 consecutive
  sessions.
- (c) unchanged: top-minus-bottom discount spread below 12%.
- Add (d) **structural:** BR_eff below 1.5 for 20 consecutive sessions
  AND net muni weight beyond ±60% — the book has collapsed into one bet.
  This is a REVIEW flag, not a kill, until P6.2 gives the group bet a
  budget.

Simulate the false-positive rate of the old clause (a) and the new (a′)
on the band backtest path with block-bootstrapped 60-session windows; put
both numbers in the note.

**5. Surface it.** `api_risk` reports `controls_armed`, each threshold,
the current value on the confirmed path, and the distance to it; the
Controls tab renders them with "observe-only" or "ARMED" in the chip.

**6. Record the governance state.** In `docs/RESEARCH_STATE.md`: controls
exist, are unarmed, the switch name, and who decides.

### Deliverables

- `risk.py` and sleeve changes, the spec keys, `ops/kill_rule.py`, the
  shared provenance function, dashboard changes.
- `results/cef/KILL_RULE_2026-09.md`: the old vs new rule, the simulated
  false-positive rates, and the exact conditions under which each clause
  fires.

### Do not

- Do not arm the controls in this prompt. `controls_armed` stays false.
- Do not compute any control on modelled NAV.
- Do not change `book_drawdown_suspend_pct` in the book spec; the units
  fix is documented and correct.
