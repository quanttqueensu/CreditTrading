# NEXT — the session of 2026-09-11 (Friday)

**Written 2026-09-10 ~11:15 ET, from the session that deployed the prod/dev
split.** This is a work order, not a research prompt: it is ordered by *when
things break*, not by value. The W-prompts hold the depth; this says which to
open and what must be true first.

**Reads first:** `00_BRIEF.md` (all of it — it is the standing brief),
`CLAUDE.md` (hard rules; note the halt section and §6.0 of
`docs/INFRASTRUCTURE.md`, both rewritten yesterday).

**Prompt filenames**, all under `docs/prompts/`: `W0` = W0_repo_hygiene.md · `W0b` = W0b_prod_dev_split.md · `W1` = W1_inference_protocol.md · `W10` = W10_breadth_and_groups.md · `W11` = W11_trading_policy.md · `W12` = W12_joint_optimiser.md · `W13` = W13_nav_quality_and_horizon.md · `W14` = W14_options.md · `W2` = W2_account_audit.md · `W3` = W3_session_architecture.md · `W4` = W4_artifact_battery.md · `W5` = W5_order_integrity.md · `W6` = W6_risk_governance.md · `W7` = W7_cost_and_turnover.md · `W8` = W8_carry_and_capacity.md · `W9` = W9_dashboard.md

---

## 0. The state you are inheriting — verify, do not assume

```
prod        ~/prod/QUANTT          worktree detached at v2026.09.10.1
dev         ~/Desktop/2027/QUANTT/2027    HEAD moves freely; reaches nothing
boundary    REPO in ~/Library/Application Support/quantt/launch_job.py
dashboard   serves ~/prod/QUANTT/dashboard/server.py on :8787
book        17 CEFs, ~$745k gross, dollar-neutral. Epoch 2026-09-08 at $500,000.
            NAV $504,573.20 at 2026-09-09.
trials      CEF 48, GAMMA 0.   tests 126.
```

Run these four before anything else and paste the output into your first reply.
If any disagrees with the above, **stop and say so** — something happened
overnight that this file does not know about.

```bash
cd ~/prod/QUANTT
git describe --tags && git status --porcelain | wc -l
python3 -m pytest -q | tail -2
python3 -m ops.doctor --quick
grep -c . ops/schedule/logs/cef_2026-09-10.log
```

---

## 1. FIRST: did last night's session work? (30 minutes, blocking)

**2026-09-10 was the first end-to-end session on the prod tree**, and four band
MOC orders were resting into that day's close:

| | | decided |
|---|---:|---|
| JFR | SELL 4,630 | on the 09-09 pair |
| MHD | BUY 115 | |
| MQY | BUY 185 | |
| NEA | SELL 4,458 | ≈ $87k gross, ~17% turnover |

These are **the book's first real band trades**. Everything before them was
either the retired 2-day-calendar policy or a pre-epoch order. Establish, in
this order:

1. **Did they fill at the 16:00 close?** `broker_fills.csv` for
   `cef_discount`, dated 2026-09-10, four symbols. If the count or the symbols
   differ from the table above, that is the finding — report it before
   continuing.
2. **Did the ledger book them?** `trades.csv` was header-only before this. It
   should now carry those fills, `orders.csv` should show them closed with a
   `fill_date`, and `positions.csv` should have moved. A fill at the broker that
   the ledger did not record is a **desync** — `ops/HALT.md` and
   `_ibkr_shadow/_desync/` will say so, and that outranks everything else in
   this file.
3. **What did they cost?** The `[slippage]` line in the log gives realised vs
   modelled bp. This is **the first honest execution measurement of the band
   policy** — one session, so no conclusion, but it starts the series `W7`
   needs. Record it; do not interpret it.
4. **Did the 17:15 session run from prod, wait for NAV, and decide?** The log
   should show the wait resolving (yfinance published at 21:45 on 09-09 and
   22:43 on 09-08 — a third data point tells you whether ~22:00 is the norm),
   then preflight, then `ARMED`, then whatever the band decided for tonight.

Write what you find into `results/cef/FIRST_BAND_SESSIONS.md` — a running
record, one short section per session, no analysis yet.

---

## 2. Make failure audible (human-gated, then 30 minutes)

Every session log still ends `[alert] email: not configured`. **This is the
single most consequential gap in the system.** The book has now had four
separate faults discovered only because a person happened to read a log:
phase0/JNK, bench_b6/ANGL, the stale ledger manifest, and a 21-session outage in
August that ran unnoticed for a month.

- **Needs the human**: a Google App Password in `config/.env` as
  `ALERT_SMTP_USER` / `ALERT_SMTP_PASS`. Ask for it once, plainly, at the top of
  your first reply. **Copy the file to prod as well** — `~/prod/QUANTT/config/.env`
  is a separate file (gitignored, so the worktree never got one; it was copied by
  hand on 09-10).
- Then send a real test through `ops.halt.alert`, confirm receipt, and make
  `ops/doctor.py::check_alerts` PASS.
- Then `W3` part C: a non-armed session must alert. It currently writes
  `ok_not_armed` to the heartbeat and raises nothing.

Do not spend the day on this if the password does not arrive — say it is
blocked, and move on.

---

## 3. Schedule the backup that the split now depends on (20 minutes)

`ops/backup_state.sh` works (tested, 164 KB, 90 archives kept) and **is not
scheduled**. Nothing runs it. That matters more than it did last week: the
promotion gate archives live state before every checkout, but between
promotions there is no copy at all, and git's copy is about to go away (item 4).

Render a launchd job for it — daily, after the evening capture, so the archive
contains the day's fills. Follow `ops/schedule/install.sh` and let the human
run `--enable`; the guard blocks `launchctl` for a reason.

---

## 4. Untrack the live ledgers (one command, then verify)

Gated on item 3, because git history is currently their only off-machine backup.

```bash
cd ~/prod/QUANTT
git rm -r --cached ops/books/cef_live ops/books/benchmarks_live \
                   ops/books/phase0_live ops/heartbeat.json
```

**The human must run this** — the project guard blocks it, correctly, because
`--cached` and a real delete look identical to a hook. The `.gitignore` stanza
is already written and explains itself. Afterwards:

- prod's tree is clean after every session, which is what the promotion gate
  wants to be able to assume;
- remove the pathspec exclusions and the `--merge` flag from `ops/promote.sh`
  and the comment that explains why they were needed, leaving a plain
  `status --porcelain` and a plain checkout. **Do not leave the workaround in
  place once the reason for it is gone** — a guard whose justification has
  expired is how a codebase acquires cargo cult.
- do the same in dev, and confirm `git status` in both trees is empty with the
  book running.

---

## 5. Give prod its own data (1 hour, do it on a non-trading day if possible)

`~/prod/QUANTT/data` is a **symlink back to the dev tree**. Prod prices the book
from panels any research script in dev can overwrite — the exact coupling the
split was supposed to end. It was left because moving 3.9 GB on a trading day
is not a thing to do casually.

The intended end state, and `ops/sync_dev_data.sh` already implements the second
half: **prod owns `data/`**, dev gets a one-way `rsync --delete` mirror. So:
move the directory into prod, replace dev's with the mirror, run the sync, and
confirm both trees see the same panel dates. Then re-run item 0's checks.

Note what this costs: a research script that writes into dev's `data/` will have
its output **deleted** by the next sync. That is the intent — research output
belongs in `results/` — but it must be written down in `INFRASTRUCTURE.md §6.0`,
which currently describes the symlink as temporary.

---

## 6. The morning decision (`W3`) — the last piece of the timing fix

Open `W3` and read it whole; it supersedes P8.1/P8.2/P0.4/P4.3 and it is written
for exactly this point.

The evidence that motivates it, now three sessions deep: sponsors publish NAV at
**21:45–22:45 ET**, so an evening decision is a race the desk does not control,
and until 2026-09-08 every session decided on the *previous* day's pair — one
extra day of decay on a signal measured at a 2-day horizon.

A decision made at 08:30 on the prior completed pair, placed MOC for that day's
close, is **the same trade** with no race. The pieces already exist
(`wait_for_nav.py`, `fetch_daily --require-asof`, the CEFConnect fallback).
What `W3` still needs built: the 08:30 + 12:00 retry schedule, the 17:30
capture-only job, and the **same-PAIR guard** replacing the same-DAY guard —
without which the morning and evening paths can decide on one pair twice and
stack two order sets in one auction.

**Promote it on a Saturday.** Not because the code is risky but because the
first fire of a new schedule should not be able to collide with a session.

---

## 7. Standing research, in the order the brief wants it

Only after 1–3 are done. Each has a W-prompt; do not start one without reading
`00_BRIEF.md` §5 (the harness) and §7 (the standing decisions).

| what | prompt | why now |
|---|---|---|
| Resolve the borrow-availability dispute | `W8` part A | **Blocking.** The retired file said NAD had 3,000 lendable shares; TWS tick 236 says 83,942. 18.7% of the short book unbuildable, or 0%. The whole availability cap turns on it and nothing may be sized until a week of paired readings settles it. Take one reading per day — it costs a minute. |
| Realised cost + turnover readout | `W7` | The pre-registered readout for the band. Every session from item 1 feeds it. |
| Arm the risk controls | `W6` | Standing decision: HALVE/KILL live on broker-confirmed NAV. The spec keys and `risk.py` still disagree on names. |
| Retire the null trader | `W2` part 2 | Removes the shared-ticker attribution faults at their source. Its control measurement is done. |
| Everything else | `W4`, `W9`–`W14` | Not before the operational work above. |

---

## What NOT to do

- **Do not run a session by hand.** The trade phase is not idempotent and there
  is no dedupe at the broker; a second armed run stacks a second order set that
  fills in the same auction. The guards will stop you — do not route around them.
- **Do not edit prod.** Edit dev, tag, promote. If you find yourself typing a
  path under `~/prod`, stop and ask why.
- **Do not re-run `W0b`.** The split is deployed; its header says so.
- **Do not treat any number in this file as an input to a decision.** They are
  dated observations. Re-measure and say what you do under either outcome.
- **Do not interpret one session's slippage.** One fill is not a cost estimate.
